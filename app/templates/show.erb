<% if grouped_events.present? %>
  Expences for <%= month %>
  <% grouped_events.each do |event| %>
    <%= event.category %> <%= pretty_currency(event.sum) %>
  <% end %>
  Total: <%= pretty_currency(total) %>
<% else %>
  No expences in <%= month %>
<% end %>

